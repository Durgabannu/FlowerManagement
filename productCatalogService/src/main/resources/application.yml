server:
  port: 8080

spring:
  application:
    name: productCatalogService

  jpa:
    hibernate:
      ddl-auto: update

  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/productserver
    username: root
    password: Bannu@123
    driver-class-name: com.mysql.cj.jdbc.Driver